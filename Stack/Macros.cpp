#include <bits/stdc++.h>
using namespace std;
#define Min(a, b) ((a) < (b) ? (a) : (b))
int main()
{
    // int area=Area(5,6);
    // cout<<area<<endl;
    // cout<<Area(1,10);
    cout << Min(5, 7);

    return 0;
}
